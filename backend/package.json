{
  "name": "calli-backend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "tsc",
    "test": "node --import tsx --env-file=.env.test --test \"test/**/*.test.ts\"",
    "seed": "tsx src/seed.ts"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.146",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
